const statusEl = document.getElementById("status");

if (statusEl) {
  statusEl.textContent = "JS novo carregado com sucesso.";
  statusEl.className = "status-box ok";
}

console.log("PAINEL NOVO CARREGADO");
